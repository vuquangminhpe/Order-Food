export enum TokenType {
  AccessToken,
  RefreshToken,
  ForgotPasswordToken,
  EmailVerifyToken
}
export enum UserVerifyStatus {
  Unverified,
  Verified,
  Banned
}
export enum MediaTypeQuery {
  Image = 'image'
}
export enum MediaType {
  Image = 'image'
}
